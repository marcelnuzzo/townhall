{% extends 'admin/base.html.twig' %}

{% block title %}Création d'un nouveau compte utilisateur{% endblock %}

{% block body %}
    <section>
        <a href="{{ path('user_index') }}" class="btn btn-outline-danger mx-3 my-3" role="button" aria-pressed="true">Retour</a>

        <div class="container">
            <div class="text-center">
                <h1>Création d'un nouveau compte utilisateur</h1>
            </div>
            
            {{ include('user/_form.html.twig') }}
        </div>
    </section>
{% endblock %}
