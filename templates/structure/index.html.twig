{% extends 'base.html.twig' %}

{% block title %}Gestion des {{ structure }}s{% endblock %}

{% block body %}
    <h1>Gestion des {{ structure }}s</h1>
	<p><a href="{{ path('structure_new', {'structure': structure }) }}">Ajouter</a></p>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Nom du logo</th>
                <th>Description</th>
                <th>Date création</th>
                <th>Voir</th>
				<th>Modifier</th>
				<th>Supprimer</th>
            </tr>
        </thead>
        <tbody>
        {% for structure in structures %}
            <tr>
                <td>{{ structure.id }}</td>
                <td>{{ structure.schoolType }} {{ structure.name }}</td>
                <td><img src="{{ vich_uploader_asset(structure, 'logoFile') }}" alt="image du logo" width="64"/></p>
                </td>
                <td>{{ structure.summar }}</td>
                <td>{{ structure.createdat ? structure.createdat|date('d/m/Y H:i') : '' }}</td></>
                <td>
                    <a href="{{ path('structure_show', {'id': structure.id }) }}">Voir</a>
					</td>
					<td>
                    <a href="{{ path('structure_edit', {'id': structure.id}) }}">Modifier</a>
                </td>
				<td>
				<a href="{{ path('structure_delete', {'id': structure.id}) }}">Supprimer</a>
				</td>
            </tr>
        {% else %}
            <tr>
				<td colspan="8">Aucun enregistrement.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
