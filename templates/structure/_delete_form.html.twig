<form method="post" action="{{ path('structure_delete', {'id': infrastructure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infrastructure.id) }}">
    <button class="btn btn-delete">Supprimer</button>
</form>
