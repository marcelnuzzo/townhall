{% extends 'admin/base.html.twig' %}

{% block title %}New Structure{% endblock %}

{% block body %}
    <section>
        <a href="{{ path('structure_index') }}" class="btn btn-delete mx-3 my-3" role="button" aria-pressed="true">Retour</a>

        <div class="container">
            <div class="text-center">
                <h1>Création d'une structure</h1>
            </div>

            {{ include('structure/_form.html.twig') }}
        </div>
    </section>
{% endblock %}
