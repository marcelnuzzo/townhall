<form method="post" action="{{ path('town_hall_delete', {'id': town_hall.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ town_hall.id) }}">
    <button class="btn">Delete</button>
</form>
